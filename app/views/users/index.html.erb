<p style="color: green"><%= notice %></p>

<h1><%= t('views.common.title_index', name: i18n_pluralize(User.model_name.human)) %></h1>

<div class="show-item">
  <ul>
    <% @users.each do |user| %>
      <li>
      <%= link_to user_path(user) do %>
        <%= user.email %>
      <% end %>
      </li>
    <% end %>
  </ul>
</div>

<%= paginate @users %>

<nav class="page-nav">
<%= link_to t('views.common.back', name: i18n_pluralize(Book.model_name.human.downcase)), books_path %>
  <%= button_to t('users.shared.links.sign_out'), destroy_user_session_path, method: :delete %>
</nav>
